<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :buyin %><br />
    <%= f.number_field :buyin %>
  </div>
  <div class="field">
    <%= f.label :prize %><br />
    <%= f.number_field :prize %>
  </div>
  <div class="field">
    <%= f.label :variant_id %><br />
    <%= f.collection_select :variant_id, Variant.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :casino_id %><br />
    <%= f.collection_select :casino_id, Casino.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
